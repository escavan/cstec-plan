# cstec-plan — 다른 노트북에서 이어가기 안내

이 zip은 **cstec-plan 프로젝트 전체 + 작업 맥락(메모리)** 을 담고 있습니다.
다른 윈도우 노트북에서 아래 순서로 복원하면 "이어서 작업"이 그대로 됩니다.

## 1) 압축 풀기
- 권장 경로: `d:\dev\cstec-plan` (원래와 같은 경로면 메모리 자동 인식이 쉬움)

## 2) 메모리 복원 ★이어가기 핵심
이 폴더(`_RESUME_memory`)의 두 파일을 아래 경로로 복사하세요:
- `cstec-plan-project.md`, `MEMORY.md`
- 복사 위치: `%USERPROFILE%\.claude\projects\d--dev-cstec-plan\memory\`
  - 폴더가 없으면 새로 만드세요.
  - 폴더명 `d--dev-cstec-plan` 은 프로젝트를 `d:\dev\cstec-plan` 에 풀었을 때 기준입니다. 다른 경로에 풀면 그 경로 기준으로 자동 폴더명이 달라집니다.

## 3) 이어가기 훅 (자동 안내 배너)
- `.claude\settings.json` 에 SessionStart 훅이 들어 있어, 새 세션을 시작하면
  **"cstec-plan 작업 이어가기"** 볼드 배너가 첫 응답에 자동으로 뜹니다.
- 동작 조건: 윈도우 + PowerShell(기본 제공). 별도 설정 불필요.

## 4) 산출물 위치
- `산출물\` : 01~04 설계 문서(.md)
  - 01 교육체계 기본설계서(v0.2) / 02 직무체계 프레임워크 / 03 과정 카탈로그 양식·샘플 / 04 신규과목 카탈로그
- `referernce\` : 분석에 쓴 원본 PDF(민감자료 — 외부 공유 금지)

## 5) 다음 할 일
1. **04 신규과목 목록에서 관련성 없는 과목 제외·선별**
2. **기존 운영 중 교육과정(현행 2026년 22개) 개선·고도화**
(이후) 단/중/장기 로드맵 → 우선순위 과목 실라버스

---
*백업 생성일: 2026-05-26*
